

/*
------------------------------------------------------------------------------------------------------------------
--Ansicht alle Daten:

SELECT * FROM benutzer 
WHERE role IN ('User', 'Admin');


------------------------------------------------------------------------------------------------------------------
--Nach genau der einen Person(User) Filtern:

SELECT * 
FROM benutzer 
WHERE id = 42
AND vorname = 'Max' 
  AND nachname = 'Mustermann'
  LIMIT 1;

------------------------------------------------------------------------------------------------------------------










------------------------------------------------------------------------------------------------------------------
-- Beide Rollenansichten Prüfen:

SELECT * FROM benutzer 
WHERE role = 'User' OR role = 'Admin';

------------------------------------------------------------------------------------------------------------------
*/